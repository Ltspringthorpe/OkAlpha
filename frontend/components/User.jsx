var React = require('react');
var ReactRouter = require('react-router');

var User = React.createClass({
  render: function () {

    return (
      <div>
        Welcome page
      </div>
    );
  }
});

module.exports = User;
